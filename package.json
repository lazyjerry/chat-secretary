{
  "name": "chat-secretary",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "hono": "^4.8.2",
    "openai": "^5.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
